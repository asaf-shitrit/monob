export const RESOLVE_ERROR = new Error(
  `Couldn't resolve the monob config using any method`
);
export const INVALID_CONFIG = new Error(`The given config was invalid`);
export const INVALID_PACKAGES = new Error(
  `One of the supplied packages were invalid`
);
